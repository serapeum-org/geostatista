# Samples

The point-observation container — a `FeatureCollection` subclass with the geostatistics method surface
(`variogram`, `interpolate_to_raster(method="kriging")` / `krige`, `cross_validate`).

::: geostatista.samples.Samples
